{
  "name": "piplusplus",
  "version": "0.1.0",
  "description": "A collection of independently configurable Pi extensions.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "test:ui": "node --test tests/ui-foundation.test.ts"
  },
  "devDependencies": {
    "@earendil-works/pi-tui": "0.82.1",
    "typebox": "1.1.38"
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./extensions/*.ts"
    ]
  },
  "dependencies": {
    "quickjs-emscripten": "^0.32.0"
  }
}
